package level2

/*
设计RandomPool结构

【题目】
设计一种结构，在该结构中有如下三个功能:
insert(key):将某个key加入到该结构，做到不重复加入
delete(key):将原本在结构中的某个key移除
getRandom ():等概率随机返回结构中的任何一个key。
【要求】
insert、 delete和getRandom方法的时间复杂度都是0(1)
*/

// insert map1=map[Index]Key  map2 = map[Key]Index
// 在delte一条数据后，那最后一条数据取填，保证哈希表内是连续的，getRandom就会很快

func insert() {

}
